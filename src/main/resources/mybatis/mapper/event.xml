<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="yongs.temp.db.mapper.EventMapper">
    <select id="getEvents" resultType="yongs.temp.vo.Event">
        SELECT * FROM `event`
    </select>
</mapper>